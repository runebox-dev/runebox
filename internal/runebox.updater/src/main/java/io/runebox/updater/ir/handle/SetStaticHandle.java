package io.runebox.updater.ir.handle;

import io.runebox.updater.ir.FieldRef;

public class SetStaticHandle extends AbstractFieldHandle {
    public SetStaticHandle(FieldRef fieldRef) {
        super(fieldRef);
    }
}
