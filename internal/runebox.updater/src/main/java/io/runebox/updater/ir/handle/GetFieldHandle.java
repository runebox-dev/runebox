package io.runebox.updater.ir.handle;

import io.runebox.updater.ir.FieldRef;

public class GetFieldHandle extends AbstractFieldHandle {
    public GetFieldHandle(FieldRef fieldRef) {
        super(fieldRef);
    }
}
