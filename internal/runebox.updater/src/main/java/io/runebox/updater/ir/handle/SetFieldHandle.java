package io.runebox.updater.ir.handle;

import io.runebox.updater.ir.FieldRef;

public class SetFieldHandle extends AbstractFieldHandle {
    public SetFieldHandle(FieldRef fieldRef) {
        super(fieldRef);
    }
}
